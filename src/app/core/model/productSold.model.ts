//productsold model
/**
 * ProductSold model
 * @export
 * @interface ProductSold
 * @property {string} id - ProductSold id
 * @property {string} name - ProductSold name
 * @property {number} quantity - ProductSold quantity
 * version 1.0.0
 * since 1.0.0
 * author: dannielf
 */
export interface ProductSold {
  id: string;
  name: string;
  quantity: number;
}
